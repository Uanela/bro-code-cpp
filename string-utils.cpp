#include <iostream>
#include <cmath>
using namespace std;
// 17

int main() {
  /*
   * length()
   * empty() - checks if empty
   * clear() - clears out the object
   * append() - concatenates
   * at(index) 
   * insert(index, text)
   * find(text)
   * erase(start_index, end_index)
   */


  return 0;
}
