// 2:26
// Make a bank ATM simulation for check balance deposit and so on with switch
#include <iomanip>
#include <iostream>

int main() {

  std::cout << std::setprecision(2) << std::fixed << 9.003;
  // When stdin fails to parse the input
  // cin:clear();
  // fflush(stdin);
  return 0;
}
